CREATE TABLE DERIVADAS_R
(
    ID NUMBER GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    FUNCION VARCHAR2(300),
    DERIVADA VARCHAR2(300),
    FACTORIZACION VARCHAR2(300),
    FECHA_REGISTRO DATE DEFAULT SYSDATE
);

COMMIT;
/*
INSERT INTO DERIVADAS_R
(
    FUNCION,
    DERIVADA,
    FACTORIZACION
)
VALUES
(
    ?,
    ?,
    ?
);*/
/*
SELECT *
FROM DERIVADAS
ORDER BY ID;*/